#Environment Variables
python-dotenv==1.0.1

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1


# 3D geometry & metrology compute
open3d==0.18.0
trimesh==4.3.2
numpy==1.26.4
scipy==1.13.0

# Point cloud formats
laspy==2.5.3          # LAS/LAZ
pye57==0.4.5          # E57

# Validation & serialization
pydantic==2.7.1
pydantic-settings==2.2.1

# Task queue (background jobs)
celery==5.4.0
redis==5.0.4

# File handling & utilities
aiofiles==23.2.1
python-magic==0.4.27
httpx==0.27.0

# Reporting
reportlab==4.2.0
pandas==2.2.2

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6


# Spacial Operations
geoalchemy2==0.15.0  # For SQLAlchemy + PostGIS integration
shapely==2.0.3       # For geometry operations (compatible with open3d/trimesh)

# Already covered by fastapi, but explicitly list if you prefer:
fastapi[all]  # Includes CORS middleware, but you're already using uvicorn[standard]

# Optional: For connection pooling (if you need it)
psycopg2-pool==2.9.9  # Not strictly necessary, but useful for some use cases

opencv-python-headless==4.9.0.80  # Headless version (no GUI dependencies)




psycopg2-binary
